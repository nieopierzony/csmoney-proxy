'use strict';

module.exports = class Util extends null {
  /**
   * Validate given IPv4
   * @param {string} ip
   * @returns {boolean} Whether given IP is valid or not
   */
  static isValidIP(ip) {
    // TODO
    return true;
  }
};
